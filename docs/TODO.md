# April 27

- [x] Sandbox may be too strict (e.g. PATH doesn't match and can't read binaries on the host e.g. to run npm check or smth)
- [x] Reading outside of user dir should actually be allowed with restrictions on par with bash tools
- [x] Pressing escape on command prompt doesn't stop an agent
- [x] Writing/deleting outside of working dir should require manual user approval

# April 26

- [x] There is no "remove" file tool, which would result in forcing agent to use dangerous_bash
- [x] Check if possible to run tests & check commands in sandbox
